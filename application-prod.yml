spring:
  application:
    name: CONFIG-PROD

eureka:
  client:
    prefer-ip-address: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
